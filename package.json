{
  "name": "zatca-invoice-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && bun install && bun run build"
  },
  "devDependencies": {
    "bun": "^1.3.0"
  }
}
